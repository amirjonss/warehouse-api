# cgi.fix_pathinfo=0
upload_max_filesize = 100M
post_max_size = 100M
memory_limit = 2G

; App serves Uzbekistan only (UTC+5, no DST), so new DateTime() and all
; timestamps run on local wall-clock time instead of the container's UTC.
date.timezone = Asia/Tashkent

