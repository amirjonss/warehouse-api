# define your env variables for the test env here
# DATABASE_URL is intentionally NOT overridden here: it is inherited from .env and
# doctrine appends the "_test" suffix (see when@test in config/packages/doctrine.yaml),
# so the suite runs against the "warehouse-api_test" database.
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
